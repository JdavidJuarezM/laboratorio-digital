import React from "react";

// Ícono del Cohete Mascota
export const RocketIcon = (props) => (
  <span {...props} role="img" aria-label="Rocket">
    🚀
  </span>
);

// Ícono para la tarjeta del Huerto
export const HuertoIcon = (props) => (
  <span {...props} role="img" aria-label="Plant">
    🌱
  </span>
);

// Ícono para la tarjeta del Supermercado
export const SupermercadoIcon = (props) => (
  <span {...props} role="img" aria-label="Shopping Cart">
    🛒
  </span>
);

// Ícono para la tarjeta de Ecosistemas
export const EcosistemasIcon = (props) => (
  <span {...props} role="img" aria-label="Globe">
    🌍
  </span>
);

// Ícono para el botón de Cerrar Sesión
export const LogoutIcon = (props) => (
  <span {...props} role="img" aria-label="Logout">
    🚪
  </span>
);

export const VocabularioIcon = (props) => (
  <span {...props} role="img" aria-label="Book">
    📚
  </span>
);

export const ReciclajeIcon = (props) => (
  <span {...props} role="img" aria-label="Recycle">
    ♻️
  </span>
);

export const ResourcesIcon = (props) => (
  <span {...props} role="img" aria-label="Resources">
    📚
  </span>
);

export const FolderIcon = (props) => (
  <span {...props} role="img" aria-label="Folder">
    📁
  </span>
);

export const FileIcon = (props) => (
  <span {...props} role="img" aria-label="File">
    📄
  </span>
);